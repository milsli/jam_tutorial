SET( INT_LIBS )

IF(WIN32)
	SET( EXT_LIBS user32 winmm)
ELSE(WIN32)
	SET( EXT_LIBS dl )
ENDIF(WIN32)

ADD_DEFINITIONS(-DJAM_EXPORTS)
BUILD_TARGET( jamcore SHARED "${INT_LIBS}" "${EXT_LIBS}" ../../include/*.h )